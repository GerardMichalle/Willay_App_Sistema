# ── PRUEBAS automatizadas: base efímera, sin semilla demo ──
spring:
  flyway:
    locations: classpath:db/migration
  jpa:
    show-sql: false

willay:
  jwt:
    secreto: secreto-exclusivo-para-pruebas-automatizadas-32-chars!
  bootstrap:
    habilitado: false

logging:
  level:
    com.willay: WARN
    org.hibernate: WARN
